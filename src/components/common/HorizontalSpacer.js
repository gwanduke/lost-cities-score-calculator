import "./HorizontalSpacer.css";

import React from "react";

const HorizontalSpacer = ({ children }) => {
  return <div className="HorizontalSpacer">{children}</div>;
};

export default HorizontalSpacer;
